# Final Project

```bash
    # install virtual
    python -m venv venv

    # install django, django-crispy-forms, pillow
    pip install django django-crispy-forms pillow

    # run server
    python manage.py runserver
```
